/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'cdn.pixabay.com',
      'via.placeholder.com',
      // add more domains as needed
    ],
  },
};

module.exports = nextConfig; 